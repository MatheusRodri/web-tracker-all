# TODO 002: Configuração do Firebase e Banco de Dados

- [ ] Criar arquivo `.env.example` com as variáveis necessárias para a API OMDB e Firebase (Regra 12 e 14: nunca mexer no `.env`).
- [ ] Instalar a dependência do Firebase (`firebase` SDK).
- [ ] Criar a configuração do Firebase client em `src/lib/firebase.ts` (inicialização do app, auth e firestore).
- [ ] Configurar regras e tipagem básicas para a persistência de séries/filmes no Firestore.
