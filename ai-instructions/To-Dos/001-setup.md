# TODO 001: Setup do Projeto Next.js e Styled-components

- [ ] Criar estrutura temporária para bootstrap do Next.js sem conflitar com `ai-instructions`.
- [ ] Inicializar o Next.js com as flags: `--ts`, `--no-tailwind`, `--app`, `--src-dir`, `--import-alias "@/*"`, `--eslint` e `--use-npm`.
- [ ] Mover os arquivos gerados para a raiz do repositório e excluir o diretório temporário.
- [ ] Configurar o styled-components no projeto (instalar dependências `styled-components`, configurar `next.config.mjs` e o registry de styles em `src/lib/registry.tsx`).
- [ ] Configurar reset de CSS global em `src/app/globals.css`.
- [ ] Testar se o styled-components está injetando estilos corretamente sem FOUC.
